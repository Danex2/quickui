type UI
@model
@auth(rules: [{ allow: owner, operations: [create, delete, update] }]) {
  id: ID!
  title: String!
  characterClass: String!
  roles: [Role!]!
  code: String!
  description: String!
  images: [String]
}

enum Role {
  TANK
  HEALER
  DPS
}
